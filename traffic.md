initially trained basic model, with a 24 lookback. 
1 simple rnn layer of 64 units. 1 dense layer of 32 with relu.
final output dense layer

wasn't perfect. so i doubled the units of the simple layer

to prevent overfitting, i slowly creep toward 